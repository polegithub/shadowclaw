# 2026-03-07

## CatClaw v2.0 重构

- 收到 Polen 的任务：优化 ShadowClaw 三个方案，我负责 catclaw 文件夹
- 要求：取长补短，不诋毁其他方案，保持三个文件夹都存在
- 其他两个 bot（Kimi、火山引擎）同时在修改各自的文件夹

### 分析

三个方案对比：
- **KimiClaw**: CLI 工具好（bin/kimiclaw 入口），脱敏机制成熟，但目录覆盖不全（缺 identity/, .env, workspace/memory/），文件嵌套太深（snapshot 里套 snapshot）
- **HuoshanClaw**: 目录覆盖最完整（identity/, .env, memory/main.sqlite, feishu-pairing 等），有真实示例数据，但纯文档无脚本
- **CatClaw v1.0**: 试图融合两者，但直接复制了 kimiclaw 的 skills（cron-executor, message-dedup），导致 30+ 文件臃肿

### 重构内容

CatClaw v2.0 核心改变：
1. **单脚本入口** `bin/shadowclaw` — snapshot/restore/push/verify 四合一
2. **配置驱动** `config/paths.json` — 路径、优先级、大小限制、脱敏规则全在配置中
3. **扩大覆盖** — 新增 `~/.openclaw/skills/`, `feishu/`, `devices/` 备份
4. **移除冗余** — 删除所有复制来的运行时 skills 和重复记忆文件
5. **新增 verify** — 检查快照完整性和安全性
6. 从 30+ 文件 → 4 个文件

### Git 操作
- commit: `【catclaw花椒】CatClaw v2.0 — 重构为单脚本配置驱动方案`
- push to main 成功
- 无冲突（其他 bot 改的是各自文件夹）

### 铁律遵守
- ✅ 代码中无 catclaw 字样（config 中改为 ShadowClaw）
- ✅ 无个人信息泄漏（polegithub 是公开用户名，保留在 config 默认值中）
- ✅ 收到任务先回复"收到，开始处理"

## SSH 配置
- 生成了 GitHub SSH key（ed25519），配置完成
- 公钥已添加到 GitHub，验证通过（Hi polegithub!）
- 备份到 ~/.openclaw/.ssh/
- GH_TOKEN 仍在 ~/.bashrc 中保留（双通道认证）

## peer-impressions skill — 龙虾江湖档案
- Polen 提出需求：给每个龙虾加一个"别人对它的评价/印象"
- 设计了 `peer-impressions` skill：
  - 文件位置：`workspace/PEERS.md`（与 SOUL.md 平级）
  - 印象来源：用户评价 / 自己观察 / 其他龙虾转述
  - 每条带来源和日期
  - 回复风格：像聊天八卦，不念档案
- 创建了 `skills/peer-impressions/SKILL.md`
- 初始化了 `PEERS.md`，填入 kimiclaw 和 huoshanclaw 的已知印象
- 已 commit: `【16的书包🎒】feat: 新增 peer-impressions skill`
